<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tabuk.app">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TabukUniverstyApp">
        <activity android:name=".AddExamActivity"></activity>
        <activity android:name=".NotificationsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ShowPlaceOnMapsActivity"
            android:label="@string/title_activity_show_place_on_maps" />
        <activity android:name=".AddLectureActivity" />
        <activity android:name=".MainManagementActivity" />
        <activity android:name=".AddNotificationActivity" />
        <activity android:name=".AddLocationActivity" />
        <activity
            android:name=".AddMapsActivity"
            android:label="@string/title_activity_add_maps" />
        <activity android:name=".MainAdminActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".ManageUsersActivity" />
        <activity android:name=".ExamsListActivity" />
        <activity android:name=".LecturesListActivity" />
        <activity android:name=".ExamsActivity" />
        <activity android:name=".LecturesActivity" />
        <activity android:name=".UpdateProfileActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".MainStudentActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".VisitorsMapsActivity"
            android:label="@string/title_activity_visitor_maps" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>